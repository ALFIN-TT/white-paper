package com.alfie.whitepaper.ui.screen.home.state

import androidx.compose.runtime.Composable
import com.alfie.whitepaper.core.ui.core.state.resolveState
import com.alfie.whitepaper.ui.screen.home.state.HomeUserState

@Composable
fun RosaryViewState(userState: HomeUserState) {
    /*userState.rosaryState.value.uiState.resolveState(
        loading = {
            userState.getRosaryLoader = true
        },
        success = {
            userState.getRosaryLoader = false
        },
        error = {
            userState.getRosaryLoader = false
        }
    )*/
}